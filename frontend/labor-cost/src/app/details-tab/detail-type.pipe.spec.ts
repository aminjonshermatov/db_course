import { DetailTypePipe } from './detail-type.pipe';

describe('DetailTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailTypePipe();
    expect(pipe).toBeTruthy();
  });
});
